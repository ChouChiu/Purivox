{
	"name": "purivox-web",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"description": "Purivox in the browser: the desktop pipelines run under Pyodide",
	"scripts": {
		"generate": "bun run scripts/build-python-archive.mjs && bun run scripts/build-i18n.mjs && bun run scripts/build-assets.mjs",
		"lint": "biome check",
		"format": "biome check --write",
		"check": "tsc --noEmit && bun run lint && bun run scripts/check-architecture.mjs",
		"predev": "bun run generate",
		"dev": "vite",
		"prebuild": "bun run generate",
		"build": "bun run check && vite build",
		"preview": "vite preview"
	},
	"dependencies": {
		"@fluentui/react-components": "^9.66.0",
		"@fluentui/react-icons": "^2.0.310",
		"react": "^19.2.0",
		"react-dom": "^19.2.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.11",
		"@types/node": "^26.4.1",
		"@types/react": "^19.2.0",
		"@types/react-dom": "^19.2.0",
		"@vitejs/plugin-react": "^5.0.0",
		"jszip": "^3.10.1",
		"typescript": "^5.9.0",
		"vite": "^7.1.0"
	}
}
